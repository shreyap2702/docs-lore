# === FastAPI server ===
fastapi~=0.116.0
uvicorn~=0.35.0

# === LangChain ecosystem (using recent compatible versions)
# Let pip determine the best compatible versions for these to avoid conflicts
langchain
langchain-core
langchain-community
langchain-google-genai

# === Google Gemini dependencies (let pip resolve compatible versions)
google-generativeai
# google-ai-generativelanguage # Usually installed as a dependency of google-generativeai,
                               # so you might not need to list it explicitly.

# === Pinecone (recent stable versions)
pinecone-client~=4.1.0
langchain-pinecone~=0.3.0

# === Vector store (optional fallback)
faiss-cpu~=1.17.0 # Updated to a more recent stable version

# === PDF and DOCX loaders
pypdf~=4.0.0 # Updated to a more recent stable version
python-docx~=1.2.0

# === Token utils & .env
tiktoken~=0.7.0 # Updated to a more recent stable version
python-dotenv~=1.0.0 # Updated to a more recent stable version
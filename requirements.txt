# === FastAPI server ===
fastapi~=0.116.0
uvicorn~=0.35.0

# === LangChain ecosystem (let pip find the latest compatible versions for these)
# Remove specific pins here if you encounter resolution issues.
# Pip will try to find the newest versions that work together.
langchain
langchain-core
langchain-community
langchain-google-genai

# === Google Gemini dependencies (let pip find the latest compatible versions)
google-generativeai

# === Pinecone (recent stable versions for good compatibility)
pinecone-client~=4.1.0
langchain-pinecone~=0.2.11 # Adjusted based on available versions to fix previous error

# === Vector store (optional fallback, needed if you use FAISS)
faiss-cpu~=1.11.0

# === PDF and DOCX loaders
pypdf~=4.0.0
python-docx~=1.2.0

# === Token utils & .env (using specific version to avoid build issues)
tiktoken==0.5.2  # Using older version with pre-compiled wheels
python-dotenv~=1.0.0